import React from 'react';

// EnvironmentChecker retained as a no-op to avoid showing environment banners in the UI.
const EnvironmentChecker: React.FC = () => null;

export default EnvironmentChecker;
